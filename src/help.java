public class help {
    public static String showMenu(String[] cmd){
        if(cmd == "help" || cmd =="he" || cmd = "?"){

        }
        else if(command == "list" || command =="li" || command =="ls" || command="l"){

        }
        else if(command =="buy") {
        }
        else if(command =="depos") {
            client.calSum(i)
        }
        else if(command =="change") {
        }
        else if(command =="admin") {
        }
        else if(command =="quit") {
        }
        else {
        }
    }
}
